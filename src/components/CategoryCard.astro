---
const { title, body, path } = Astro.props
---

<div class="flex flex-col gap-4">
  <div class="aspect-[1/1] relative overflow-hidden rounded-lg">
    <img class="w-full h-full asolute inset-0" src={path} alt={title}>
  </div>
  <div>
    <h4 class="text-sm font-medium text-slate-900">{title}</h4>
    <p class="text-xs font-medium mt-1.5 text-slate-500">{body}</p>  
  </div>  
</div>